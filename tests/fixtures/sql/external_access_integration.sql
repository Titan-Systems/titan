CREATE OR REPLACE EXTERNAL ACCESS INTEGRATION SOME_EXTERNAL_ACCESS_INTEGRATION
  ALLOWED_NETWORK_RULES = (STATIC_DATABASE.PUBLIC.STATIC_NETWORK_RULE)
  COMMENT = 'Example external access integration for demonstration purposes'
  ENABLED = true;