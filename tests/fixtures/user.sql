CREATE USER IF NOT EXISTS jill
    PASSWORD = 'p4ssw0rd'
    DEFAULT_ROLE = PUBLIC
    DEFAULT_WAREHOUSE = XSMALL_WH
    MUST_CHANGE_PASSWORD = FALSE
;

CREATE USER user1 PASSWORD='abc123' DEFAULT_ROLE = myrole DEFAULT_SECONDARY_ROLES = ('ALL') MUST_CHANGE_PASSWORD = TRUE;